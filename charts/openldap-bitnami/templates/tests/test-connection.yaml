apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openldap-bitnami.fullname" . }}-test-connection"
  labels:
    {{- include "openldap-bitnami.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openldap-bitnami.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
